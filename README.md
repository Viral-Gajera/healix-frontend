# Healix Frontend

```
- I want to create a chat bot application just like chatgpt or gemini for health care.
- The name of the application is Healix.
- Where user can login or signup (its first page of the application)
- Use can have multiple chats, delete chat, ai chat windows, have memory, attach files in chat etc features.
- No need to create backend for now! I'll create backend later. You can use mock data for now / json fake server.
- User can switch between light and dark mode.
- Remove boilerplate code and create a clean UI for the application.
```

# Next.js template

This is a Next.js template with shadcn/ui.

## Adding components

To add components to your app, run the following command:

```bash
npx shadcn@latest add button
```

This will place the ui components in the `components` directory.

## Using components

To use the components in your app, import them as follows:

```tsx
import { Button } from "@/components/ui/button";
```
